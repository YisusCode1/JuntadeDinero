import React from 'react';

const Account: React.FC = () => {
    return (
        <div>
            <h1>Cuenta</h1>
            {/* Lógica para manejar la cuenta del usuario */}
        </div>
    );
};

export default Account;

